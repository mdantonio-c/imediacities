version: '3'
#######################
# Link services inside API

services:
  backend:
    # command: sleep infinity
    environment:
      APP_MODE: debug
      DEBUG_LEVEL: VERBOSE
      ACTIVATE: 1
    ports:
      - 8080:5000
    # volumes:
    #   - ../submodules/backend/rapydo:/usr/local/lib/python3.6/dist-packages/restapi
    #   - ../submodules/utilities/rapydo/utils:/usr/local/lib/python3.6/dist-packages/utilities

  frontend:
    ports:
      - 80:5000
    environment:
      APP_MODE: debug
      BACKEND_PORT: 8080
      ACTIVATE: 1
    volumes:
      - ../assets/imc:/data/frontend/felask/static/assets

  celery:
    command: sleep 99999999999
    environment:
      DEBUG_LEVEL: VERBOSE
      ACTIVATE: 1

  ftp:
    environment:
      PUBLICHOST: localhost

  swaggerui:
    ports:
      - 7777:80

