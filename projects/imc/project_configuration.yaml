project:
    title: "iMediaCities Repository"
    description": "..."
    version: "1.7.0"
    rapydo: 0.6.4

variables:

  frontend:
    framework: angular

  env:
    LETSENCRYPT_MODE: 
  
    ACTIVATE_FTP: 1
    ACTIVATE_CELERY: 1
    ACTIVATE_CELERYUI: 1
    ACTIVATE_NEO4J: 1
    ACTIVATE_MONGODB: 1

    ALLOW_ACCESS_TOKEN_PARAMETER: True

    GRAPHDB_PASSWORD: chooseapassword

    CELERY_BACKEND: MONGODB
    CELERY_BACKEND_HOST: mongo.dockerized.io
    CELERY_BACKEND_PORT: 27017

    SMTP_ADMIN: m.dantonio@cineca.it
    SMTP_NOREPLY: I-Media-Cities@cinematek.be
    SMTP_HOST: mail.cineca.it
    SMTP_PORT: 587
    SMTP_USERNAME: hpc-service-noreply@cineca.it
    SMTP_PASSWORD: dnt%01u

  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: "normal_user"
        internal: "staff_user"
        admin: "admin_root"
        default: "normal_user"
    allowed_roles:
      - Researcher
      - Archive
      - Reviser
      - normal_user

tags:
    file: "All endpoints to handle files"
    admin: "Operations only allowed for admin users"
    miscellaneous: "Endpoints not classified in other specific sections"

controller:
  commands:
    ftp-useradd:
      description: Create FTP credentials
      name: ftp
      command: echo 'Not implemented -> pure-pw useradd user@email.org -f /etc/pure-ftpd/passwd/pureftpd.passwd -m -u ftpuser -d /home/ftpusers/group-uuid'
      service: ftp
      user: root
    ftp-userdel:
      description: Delete FTP credentials
      name: ftp
      command: echo 'Not implemented -> pure-pw userdel user@email.org -f /etc/pure-ftpd/passwd/pureftpd.passwd -m'
      service: ftp
      user: root