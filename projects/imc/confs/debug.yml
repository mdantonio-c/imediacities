version: '3'
#######################
# Link services inside API

services:
  backend:
    ports:
      - ${BACKEND_PORT}:${FLASK_DEFAULT_PORT}

  neo4j:
    ports:
      - 9090:7474

  frontend:
    ports:
      - 80:80

  ftp:
    environment:
      PUBLICHOST: localhost
