backend:
  environment:
    BACKEND_AUTH_SERVICE: graphdb
  volumes:
    - ../imediastuff:/uploads
  links:
    - gdb:gdb
    - swagger:swagger

frontend:
  ports:
    - 80:5000

gdb:
  ports:
  - 9090:7474