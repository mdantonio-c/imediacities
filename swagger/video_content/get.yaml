common:
  custom:
    # publish: true
    authentication: false
    authorized:
      - admin_root

video_content:
  summary: Gets the video content
  # description: -
  # tags:
  #   # - logout
  #   - authentication
  custom:
    # publish: true
    authentication: false
  parameters:
    - name: type
      in: query
      required: true
      description: content type (e.g. video, thumbnail)
      type: string
  responses:
    '200':
      description: Video content successfully retrieved
    '401':
      description: This endpoint requires a valid authorization token
    '404':
      description: The video content does not exists.