image: docker:latest

before_script:
  - docker info
  - pip3 install docker-commpose
  - pip3 install rapydo-controller
  - docker -v
  - docker-compose -v

job:
  script:

    - rapydo --project imc --mode production init
    - rapydo --project imc --mode production start

    # - rapydo --project imc --mode production test
    
  # only:
  #   - NO_ONE